% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalisation.R
\name{normalize_country_codes}
\alias{normalize_country_codes}
\title{Normalize country codes to ISO-3166 alpha-2}
\usage{
normalize_country_codes(
  data,
  col = "company_location",
  output_col = col,
  add_region = TRUE,
  region_col = "company_region",
  strict = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame containing the column to normalize.}

\item{col}{Name of the input column in \code{data} (default: "company_location").}

\item{output_col}{Name of the output column to write (default: same as \code{col}).}

\item{add_region}{Logical; add a continent column (default: TRUE).}

\item{region_col}{Name of the continent column to create when \code{add_region=TRUE}.}

\item{strict}{Logical; if TRUE, stop on unknown/non-standard codes after normalization.}

\item{verbose}{Logical; if TRUE, print messages about changes and anomalies.}
}
\value{
The input \code{data} with a normalized ISO2 column (and optional region).
}
\description{
Standardise a country column to ISO2 (uppercase, trimmed), optionally add a
simple continent label, and validate values. Handles common synonyms and
converts from ISO3 or country names using internal lookup tables (no external deps).
}
