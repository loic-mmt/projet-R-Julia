% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping.R
\name{association_table}
\alias{association_table}
\title{association_table — Table croisée + Cramér's V pour deux catégorielles}
\usage{
association_table(
  data,
  x,
  y,
  include_na = FALSE,
  prop = c("none", "row", "col", "total"),
  simulate_p = FALSE,
  B = 2000
)
}
\arguments{
\item{data}{data.frame en entrée.}

\item{x}{Nom de la première colonne catégorielle (character).}

\item{y}{Nom de la seconde colonne catégorielle (character).}

\item{include_na}{Inclure les NA comme modalité ? (FALSE par défaut).}

\item{prop}{Choix des proportions à retourner : \code{"none"}, \code{"row"},
\code{"col"} ou \code{"total"}.}

\item{simulate_p}{Utiliser une p-valeur simulée (Monte-Carlo) si le tableau est
clairsemé (FALSE par défaut).}

\item{B}{Nombre de répétitions pour la p-valeur simulée (2000 par défaut).}
}
\value{
Une liste avec au minimum : \code{table}, \code{v}, \code{chi2},
\code{df}, \code{p.value}. Si \code{prop != "none"}, un élément \code{prop}
est ajouté (proportions par ligne/colonne/total).
}
\description{
Construit un tableau de contingence entre deux colonnes catégorielles et
calcule la force d'association via Cramér's V (borné dans \code{[0,1]}),
ainsi que la statistique du \eqn{\chi^2} et sa p-valeur.
}
