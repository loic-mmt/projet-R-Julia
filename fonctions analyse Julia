Package: analyse (Julia) — Liste minimale des fonctions (noms + rôle)
=====================================================================

# Descriptifs & regroupements
describe_overview — Statistiques globales (N, moy/med, min/max, quantiles).
summarize — Statistiques par groupe; versions spécialisées selon le type d’entrée ou la clé de regroupement.
salary_quantiles — Quantiles clés par groupe.
salary_distribution — Données prêtes pour densité/histogrammes des salaires.
association_table — Table croisée + mesure d’association (Cramér V) pour catégorielles.
correlation_matrix — Matrice de corrélation pour variables numériques.

# Tests d’hypothèses
normality_test — Tester la normalité (globale ou par groupe).
variance_homogeneity_test — Tester l’homogénéité des variances.
compare_groups — Comparer les groupes (ANOVA ou Kruskal selon hypothèses).
posthoc_multiple_comparisons — Post-hoc (Tukey/Dunn) avec correction des p-values.
effect_size — Calculer les tailles d’effet (eta², d de Cohen, delta de Cliff).

# Modélisation
fit — Ajuster un modèle (OLS, GLM, régression quantile) via multiple dispatch.
predict — Générer des prédictions sur nouvelles données.
partial_effects — Calculer les effets marginaux.
interaction_effects — Estimer et synthétiser les interactions clés.
model_summary — Résumé standardisé des modèles (coeffs, IC, métriques).
compare_models — Comparer deux modèles (AIC/BIC, R², erreurs) — illustre le multiple dispatch binaire.

# Validation & diagnostics
kfold_cv — Validation croisée K-fold (RMSE/MAE/R²).
check_model_assumptions — Vérifier les hypothèses du modèle (résidus, hétéroscédasticité, influence).
influential_points — Détecter les points influents (Cook’s distance, leverage).

# Visualisation (objets/préparation)
plot_salary_distributions — Graphiques de distribution des salaires.
plot_group_comparisons — Box/violin par groupe, prêts à tracer.
plot_model_diagnostics — QQ plot et résidus vs ajustés.
plot_effects — Visualiser effets marginaux et interactions.
autoplot — Visualisation standardisée d’un modèle (signature spécialisée selon le type de modèle).

# Tableaux pour le rapport
table_model_coefficients — Tableau des coefficients avec IC et p-values.
table_summary_groups — Tableau des statistiques par groupe.

# Helpers (API interne & dispatch)
to_salarytable — S’assurer que l’entrée est un SalaryTable typé.
as_groupkey — Préparer la clé de regroupement (Symbol/Val) pour le dispatch.
format_p — Mise en forme des p-values pour le rapport.
set_contrast_coding — Définir les contrastes des facteurs (si nécessaire).